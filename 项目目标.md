镜像源：http://mirrors.aliyun.com/pypi/simple/

使用多模态大模型

监控抽帧 -> 安全监测(支持边界框+文字标注) -> 写入数据仓库 -> 人工审核 -> 高质量数据 -> 更好的模型

标注准确率低了
这是为什么？

看来要拉自己的代码仓库了，现在代码烂掉了，可恶啊！

果然稳定就不要乱改代码，会出问题的
小模型测试

1.优化导入docx\excel 文档
2.美化Gradio界面，给出更多案列演示
3. ppt 演示

from core.agent import MultClient
import asyncio
from dotenv import load_dotenv
from export.export_docx import export_to_word,export_to_excel
import os
load_dotenv()


if __name__ == "__main__":
    api_key:str = os.getenv("api_key")
    base_url:str = os.getenv("base_url")
    model_name:str = os.getenv("model")
    example:list[str] = ["未固定的高空作业平台","裸露的带电电缆"]
    image_path = "image/6.jpg"
    ima_list = ['image/5.jpg','image/6.jpg','image/7.jpg','image/8.jpg','image/9.jpg']

    client = MultClient(api_key,base_url,model_name,example)

    async def test() -> None:
        example_data:list = await client.batch_infer(ima_list,True)
        #print(example_data)
        export_to_excel(example_data)

    asyncio.run(test())


    async def infer(self, image_path: str | Image.Image, is_label: bool) -> dict[str, Image.Image | str]:
        # 兼容路径字符串和 Pillow 图片对象，并确保统一转换为 RGB 模式
        image = Image.open(image_path).convert("RGB") if isinstance(image_path, str) else image_path.convert("RGB")
        
        results = await self.secure_check(image)
        
        # 提取并缓存 keys 和 values，避免后续多次重复调用 list()
        keys = list(results.keys())
        values = list(results.values())

        output_image = self.visualize_boxes(image, values, keys, renormalize=True) if is_label else self._encode_image_data(image)
        label_text = "\n".join(f"{i}.{lbl}" for i, lbl in enumerate(keys, start=1))

        return {"image": output_image, "label": label_text}



**Role**：
        you is a Safety Inspector

        **Target**：
        1. Identify potential safety hazards in the provided image, with a specific and primary focus on workers' standardized operations and safe work practices. Key areas of concern include, but are not limited to:
        - Workers performing edge work or working at heights without properly fastened safety harnesses or lifelines.
        - Use of non-compliant, damaged, unstable, or unsafe scaffolding (e.g., missing guardrails, toe boards, or improper assembly).
        - Improper, unsafe, or unauthorized operation of aerial work platforms (e.g., scissor lifts, boom lifts), such as standing on guardrails or overreaching.
        - Lack of appropriate Personal Protective Equipment (PPE), such as hard hats, high-visibility vests, or safety shoes.
        - Any other unsafe working conditions or behavioral violations of standard safety protocols.
        2. Annotate each identified safety hazard with its precise 2D bounding box coordinates.
        3. The description of each hazard (the "label") MUST be written in Chinese.

        **Limit**：
        1. The bbox_2d format is [x1, y1, x2, y2]
        2. Output format: JSON

        **JSON format case**：
        [
            {{
                "bbox_2d": [120, 85, 340, 290],
                "label": "工人临边作业未正确佩戴或悬挂安全带"
            }},
            {{
                "bbox_2d": [450, 210, 620, 500],
                "label": "使用不合规且缺乏防护栏杆的脚手架"
            }}
            ....
        ]



你是一名专业的施工现场安全巡查员，负责识别图像中工人的不安全行为和违章作业行为。

请只关注“工人自身的不安全行为”，不要重点检查设备、材料、环境或管理问题。只有当某个物体或环境与工人的危险行为直接相关时，才可以一并纳入边界框。

【重点识别行为】
- 高处作业、临边作业、洞口作业未佩戴安全带或未正确挂扣安全带。
- 高处作业安全带未高挂低用，或安全带悬挂方式明显错误。
- 工人站在脚手架、平台、梯子、设备边缘、护栏、临边位置进行冒险作业。
- 工人攀爬、跨越、倚靠、坐卧在护栏、脚手架杆件、洞口边缘或不稳定位置。
- 工人在升降平台、剪叉车、曲臂车等设备上探身、跨越、站立在护栏上或超出安全作业范围。
- 工人未戴安全帽，或未正确系紧安全帽下颚带。
- 工人在施工区域未穿反光背心或未佩戴明显必要个人防护用品。
- 其他明显违反安全操作规程的工人行为。

【输出要求】
1. 每个不安全行为单独输出一条 JSON 对象。
2. 边界框格式为 bbox_2d: [x1, y1, x2, y2]，使用整数像素坐标。
3. 边界框应紧密包围涉事工人及其危险动作范围。
4. label 必须使用中文，简明描述工人的具体不安全行为。
5. 只标注明确可见、可判断的违章行为；不确定、模糊、遮挡严重导致无法判断的情况不要输出。
6. 只输出 JSON 数组，不要输出任何解释、Markdown 或额外文字。
7. 如果没有发现工人不安全行为，输出 []。

【JSON 格式】
[
    {
        "bbox_2d": [x1, y1, x2, y2],
        "label": "工人临边作业未正确佩戴或挂扣安全带"
    }
]


web 应用后端涉及的接口有
1.上传图片
2.目标检测图片生成html表格
3.配置模型
4.获取模型配置
5.导出docx文件


该文件是使用pywebview构建的简单测试模型安全检测小应用，现在涉及到对应代码有些更改，现在请你完善该文件，涉及到接口（或者方法）如下：
1.上传图片
2.目标检测图片生成json数据，
3.配置模型
4.获取模型配置
5.导出docx文件


界面有三个
1.首页界面 -> 标题 + 两个按钮，一个是模型配置 ，一个安全隐患检测
2.模型配置界面
3.安全隐患检测结果界面（绑定安全隐患检测按钮，安全检测完成后显示，为安全隐患表）,该页面有导出按钮（只能导出excel和docx，导出效果自动打开导出文件）
